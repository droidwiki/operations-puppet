input {
  file {
    path => "/var/log/nginx/*access*"
    exclude => "*.gz"
    type => "nginx"
    tags => ["nginx", "nginx-access"]
  }

  file {
    path => "/var/log/nginx/*error*"
    exclude => "*.gz"
    type => "nginx"
    tags => ["nginx", "nginx-error"]
  }
}
