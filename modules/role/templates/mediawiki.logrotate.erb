# logrotate(8) config for mediawiki
# This file is managed by Puppet

# Delete logs after 3 days, the logs are saved in logstash anyway
/data/log/mediawiki/*.log {
    su <%= @user %> <%= @group %>
    rotate 0
    maxage 3
    missingok
}
