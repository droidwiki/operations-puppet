# Upstart job configuration for cxserver

description "cxserver service"

start on (local-filesystems and net-device-up IFACE!=lo)
stop on runlevel [!2345]

setuid "www-data"
setgid "www-data"

env NODE_PATH="//data/mediawiki/services/cxserver/node_modules"

chdir "/data/mediawiki/services/cxserver"
exec npm start

respawn
